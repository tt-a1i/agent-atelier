# Archive mine-delta absorb pass (top 3)

**Date:** 2026-07-21 · Map [#34](https://github.com/tt-a1i/agent-atelier/issues/34) · Sweep [#60](https://github.com/tt-a1i/agent-atelier/issues/60)  
**Queue source:** [`archive-still-teaches-sweep.md`](./archive-still-teaches-sweep.md) priority list  
**Rule:** Archive is not Current; harvest design encodings; cite Current code/tests in Destination bodies.

## Absorbed this pass

### 1. `runtime-mainline-teaching-manual.md` → §01 deltas

| Harvested encoding | Where landed | Current authority |
| --- | --- | --- |
| Turn / Run / Event ledger as three mental models (not synonyms) | §01 `#identities` archive aside + outline note | RuntimeEvent model + AgentRun header |
| “Missing a layer doesn’t break immediately — it squeezes responsibility into the wrong place” | §01 `#exec-spine` pedagogical failure mode | Live spine table |
| Shared core loop vs product shells (Desktop / headless / Harbor) must not each rewrite the agent loop | already in §01 dual-stream / §04 Harbor; reinforced in outline | headless + runtime packages |

**Not absorbed as Current:** layer names or call-graph snapshots that diverge from today’s `ARCHITECTURE*` / code.

### 2. `deepseek-reasonix-cost-runtime-design.md` → cost spine deltas

| Harvested encoding | Where landed | Current authority |
| --- | --- | --- |
| Cost line is discipline on existing spine — not a `CostRuntime` | §01 `#request-shape` archive honesty + [`usage-cost-telemetry-deep-outline.md`](./usage-cost-telemetry-deep-outline.md) | request-shape diagnostics + usage stores |
| Four questions: provider-visible request / hit-miss-write / which prefix changed / evidence after savings | already core of `#request-shape`; archive labeled as narrative mine-delta only | code + §01 |
| Anti-pattern: shorter history can raise cache miss + estimated cost | §01 cost failure stories (kept) | provider usage attribution tests |

**Not absorbed:** open decisions / paid-matrix TODOs that may be stale vs Current.

### 3. CU hardening suite (delivery + evidence + executor/runtime notes) → `/guides/computer-use`

| Harvested encoding | Where landed | Current authority |
| --- | --- | --- |
| `outcome_unknown` vs `capture_failed` (delivered but unverifiable) | CU guide `#failures` + `#evidence-hardening` | computer-use packages + runtime openai-computer tests |
| Unified report sanitizer; missing evidence = invalid/inconclusive, never pass | CU guide new subsection | evidence-classes + sanitizer |
| Observations enter FIFO only after screenshot normalization | CU guide failure matrix note | foundation contract + tests |

**Not absorbed:** PR chronicles / e2e harness diaries (history-only in sweep table).

## Remaining mine-delta (not this pass)

`heavy-task-mainline-system-design.md`, `maka-memory-whitebox-contract.md`, `runtime-v2-architecture-evolution.md`, `runtime-kernel.md`, `maka-core-tech-walkthrough.md`, `economic-mechanisms-benchmark.md`, archive `README.md` inventory — still queued.
