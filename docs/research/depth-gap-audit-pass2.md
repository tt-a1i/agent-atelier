# Cognitive-track depth gap audit — pass 2

**Date:** 2026-07-21  
**Map:** [#34](https://github.com/tt-a1i/agent-atelier/issues/34)  
**Prior:** [`depth-gap-audit.md`](./depth-gap-audit.md)  
**Corpus raise:** [`maka-corpus-mine-plan.md`](./maka-corpus-mine-plan.md)  
**Live:** https://tt-a1i.github.io/agent-atelier/

## Verdict (harsh)

**Destination is NOT complete.**

Pass 1 measured six ARCHITECTURE chapter essays against six architecture drafts. After protocol rewrites (§01 `6eeef22`, §02 prune, §03 compaction, §04–§06 headless/self-check/AHE), those essays are **no longer slogan posters** for their named invariants.

Pass 2 raises the bar to the **entire maka-agent corpus** (127 first-party markdown files + design-bearing packages). Under that bar, atelier has mined **one cluster** and left **most fat seams empty**. Claiming Destination done would be the same class of fake completion the user already rejected.

---

## What is now deep (six-chapter cluster only)

Spot-check of live CN chapters vs pass-1 gaps:

| Former gap | Live status |
| --- | --- |
| Active vs stale prune + archive-before-placeholder + fail-open | §02 protocol sections + figures — **deep** |
| Compaction schema / prior-messages pipeline / fail-open | §03 — **deep** |
| Cache / prefixHash / request-shape | §01 `#request-shape` hosts teaching; §02/§03 lean pointers — **deep as planned home** |
| Log spine / dual stream / terminal race / permission-as-action | §01 — **deep** |
| TaskRun budgets / park / cold-start honesty | §04 — **deep for TaskRun** |
| Self-check Source Guard / repair budget / advisory forever | §05 — **deep** |
| AHE export / readonly evidence / falsifiable manifest | §06 — **deep on boundary**; Target controller still honest stub |

Pass-1 “acceptance of depth done” for **those named invariants** is largely met inside §01–§06. That is necessary and **insufficient**.

---

## Remaining holes (honest)

**Superseding status file:** [`depth-corpus-status.md`](./depth-corpus-status.md) (2026-07-21 evening). Pass-2 list below is historical; do not treat as live empty set.

### A. Corpus-empty (atelier silence) — largely cleared

Former empties (sandbox, CU, ledger, evidence spine, MCP, swarm, skills, shell/FS, bots, privacy, desktop-host, …) are **shipped**. Live empties among Cluster 1 fat seams: **none**. Remaining are **lean**, not empty.

### B. Still thin / lean

- §01 `#stores` / durability — lean vs full storage matrix  
- §01 `#usage-stores` — dual-store present; capture/scrub thin  
- §01 `#runtime-host` — lean note  
- Archive mine-delta remainder (beyond top-3 absorb)  
- AHE Target controller / runner — honest stub  

### C. Entry / chrome debt

- Entry copy updated to point at `depth-corpus-status.md` and refuse Destination-complete claims  
- Hard-gate Done ≠ research-report depth (still true)

### D. Out of Destination unless IA expands

Frontend CSS, DESIGN.md polish, 29 bundled SKILL bodies, eval score writeups — inventoried, not pretend-mined.

---

## Relation to map Decisions

- [#35](https://github.com/tt-a1i/agent-atelier/issues/35) even-depth across six chapters: **chapter essays advanced**; corpus raise **adds** orthogonal seams.  
- Do **not** close #34.  
- Mine plan is the working backlog; research tickets must cite **code + tests**, not docs alone.

---

## Pass-2 acceptance (updated)

Destination complete only when:

1. Six-chapter invariants stay deep (pass-1 bar).  
2. Fat corpus seams in mine-plan Cluster 1 reach **deep** or an explicit companion page — not empty.  
3. Archive-that-still-teaches is either mined or explicitly deferred with reason.  
4. Entry copy stops equating checklist Done with research-report depth.

Until then: **depth pass in progress.**
