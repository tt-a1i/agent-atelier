# Corpus depth status (honest)

**Date:** 2026-07-21  
**Map:** [#34](https://github.com/tt-a1i/agent-atelier/issues/34)  
**Against:** [`maka-corpus-mine-plan.md`](./maka-corpus-mine-plan.md) · [`maka-code-seam-inventory.md`](./maka-code-seam-inventory.md)  
**Live:** https://tt-a1i.github.io/agent-atelier/

## Verdict

**Destination is NOT complete.**

Ticket frontier under #34 is **nearly empty** again after shipping companion adversarial reread (#83) + §06 AHE protocol deepen (#84) + local-memory companion (#85). That is **still not** the same as corpus-complete Destination. Named fat seams + protocol deepen ≠ every outline+shipped companion raised to failure-story **deep**, and Destination bar on the map remains open until a hostile reader can teach every Cluster 1 seam’s failure modes from atelier alone *and* entry chrome stops implying “depth pass ≈ Destination.”

**Honest leftover after this pass:** mostly **polish** (companion failure-story enrichment, optional `/guides/cost` / storage / runtime-host spin-outs) + **product Target stubs** (AHE controller / runner — correctly not invented). Do **not** close #34.

## Method (this pass)

1. Hostile reread of companions + chapter lean pointers vs maka Current code/tests (#83) — **no confirmed factual wrongs**; fixed Discord/QQ manual-only honesty + sandbox README lag caveat.  
2. Deepen §06 against `ahe-target-protocol.ts` / docs / tests (#84) — snapshotId inputs, validator matrix, ChangeEvaluation type-without-validator; controller stub kept.  
3. Mine Current local-memory (`local-memory.ts` + Desktop service + system-prompt inject) → `/guides/local-memory` CN+EN (#85); §01 pointer updated.

Depth classes (same as mine-plan):

| Class | Meaning |
| --- | --- |
| **deep** | Reader can restate protocol / failure modes from atelier alone |
| **outline+shipped** | Outline + live narrative home (may still have Target stubs) |
| **lean** | Present but short; protocol matrix thin |
| **empty** | Absent / near-absent on site |

## Cluster scores (mine-plan buckets)

### Cluster 0 — Six architecture chapters

| Seam | Status |
| --- | --- |
| §01 log / terminal / permission | **deep** |
| §01 `#request-shape` / cache | **deep** |
| §02 prune / evidence | **deep** |
| §03 compaction | **deep** |
| §04 TaskRun | **deep** |
| §05 self-check | **deep** |
| §06 AHE boundary | **deep** on Current protocol surface (validators + identity + evaluation type); Target controller / runner / `validateMakaAheChangeEvaluation` still stub |

**Bucket:** ~100% of named six-chapter invariants are **deep** for their named claims.  
**Not** a Destination claim — see Cluster 1 remainder / Destination bar.

### Cluster 1 — Fat contracts / code seams

| Seam | Status (honest) |
| --- | --- |
| Sandbox | **deep** |
| Computer-use | **outline+shipped** (`/guides/computer-use`) · hostile pass OK |
| Recovery Phase0/1 + T1/T2 Resolver | **deep** |
| AgentRunRecovery vs RuntimeRecovery | **deep** |
| Execution-evidence spine | **deep** |
| Session task ledger ≠ TaskRun | **deep** |
| MCP runtime | **outline+shipped** · hostile pass OK |
| Agent swarm + expert team | **outline+shipped** · hostile pass OK |
| Skill catalog policy | **outline+shipped** · hostile pass OK |
| Headless Harbor trust | **outline+shipped** · hostile pass OK |
| Shell / PTY / workspace executor | **deep** |
| Filesystem worker / path containment | **deep** |
| Desktop main composition | **deep** (`/guides/desktop-host`) |
| Bot / IM onboarding | **deep** (`/guides/bot-im` · Discord/QQ manual-only note this pass) |
| Workspace privacy + SECURITY | **outline+shipped** (`/guides/security-privacy` · sandbox README lag caveat) |
| ContextBudgetPolicy DSL | **deep** |
| Semantic compact gates | **deep** |
| Park / remember vs escalation | **deep** |
| Autonomous budget × projection | **deep** |
| Heavy-task gate → official verifier | **deep** |
| AHE scoreAuthority + forbidden patch | **deep** |
| AHE Target protocol validators / identity | **deep** (this pass · §06; controller still Target stub) |
| **Storage ledgers (JSONL/SQLite/artifacts)** | **deep** |
| **Usage / cost telemetry beyond request-shape** | **deep** |
| **Runtime-host gateway** | **deep** |
| **Local memory whitebox** | **deep** (this pass · `/guides/local-memory` from Current code) |

**Approx. Cluster 1:** ~27 deep / outline+shipped · **0 lean** · **0 empty** among listed fat seams.  
Rough: **~100% deep or outline+shipped** for the named fat list — still **not** Destination (companions may stay outline+shipped for polish; Target controller stubs; Destination bar).

### Cluster 2 — Archive that still teaches

| Item | Status |
| --- | --- |
| Top-3 mine-delta absorb | **done** |
| Batch-2 (≥5) absorb | **done** |
| Remaining mine-delta | archive `README.md` inventory only (keep as index) |
| Memory whitebox | **absorbed to companion** — archive delta remains history aside |
| History-only / discard | classified in `archive-still-teaches-sweep.md` |

**Bucket:** classification complete; design-bearing mine-delta **absorbed or deferred-with-reason**.

### Cluster 3 — Lower priority

Frontend CSS / DESIGN.md / 29 SKILL bodies / eval score writeups — inventoried, **out of Destination** unless IA expands.

## Remaining thin / fog (do not close #34 for these alone)

1. **Optional `/guides/cost`** — only if `#usage-stores` + `#request-shape` prove insufficient (currently fog; not required).  
2. **Optional `/guides/storage-ledgers` / `/guides/runtime-host`** — §01 now protocol-deep; companions optional if chapter explodes.  
3. **AHE Target controller / runner / change-evaluation validator** — correctly stubbed; incomplete closed loop (product Current).  
4. **outline+shipped companions** (CU, MCP, swarm, Harbor, privacy) — hostile pass found **no factual wrongs**; leftover is polish / richer failure stories, not honesty blockers.  
5. **Entry / map copy** — keep saying Destination not complete / depth in progress.

## Ticket frontier

- Open wayfinder issues at status write: **#34 only** (map), after closing #83 / #84 / #85.  
- Children #35–#85 closed for shipped research/tasks (numbers may skip).

## What would make Destination true

1. ~~Cluster 1 lean trio (storage / usage / runtime-host) reach **deep**~~ — **done**.  
2. ~~Archive mine-delta remainder either absorbed or explicitly deferred with reason~~ — **done**.  
3. ~~Companion hostile-reader pass for factual wrongs~~ — **done this pass** (no confirmed wrongs; thin spots fixed).  
4. Entry / map copy stop implying “depth pass ≈ Destination” — **still required**; keep honest WIP.  
5. Optional: raise remaining outline+shipped companions from polish → failure-story **deep** if Destination bar demands it.  
6. Optional: close remaining Target stubs only when product Current catches up — do not invent.

Until then: **depth pass advanced — Destination not complete.** Leftovers after this pass are largely **polish + product Target stubs**, not missing named fat seams.
